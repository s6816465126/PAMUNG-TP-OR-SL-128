rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // User profile and trade subcollections
    match /users/{userId} {
      allow read, write: if true;

      match /{allChildren=**} {
        allow read, write: if true;
      }
    }
  }
}
